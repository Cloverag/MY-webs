#include<stdio.h>

struct my Array
{
    int total _size;
    int used_size;
    int *ptr; // array elemnt 
};
void createArray(struct myArray* a) //abstract data type
{
    (*a).total_size = tSize;
    (*a).used_size = uSize;
    int* ptr =(int *) malloc(tsize*sizeo(int);//
}


int main()
struct  myArray marks;
createArray(marks,100,20);

return 0;
}
